# Keep cloud runtime dependencies lightweight and Python 3.14 compatible.
# AkShare, baostock and FastAPI are optional local-development extras declared
# in pyproject.toml; the hosted Streamlit app uses bundled seed data plus curl.
numpy>=2.3,<3
pandas>=2.3,<4
plotly>=6.3,<7
requests>=2.32,<3
streamlit>=1.50,<2
